
import { useToast as useToastHook, toast as toastFunction } from "@/hooks/use-toast";

// Export the hook and function with proper types
export const useToast = useToastHook;
export const toast = toastFunction;
